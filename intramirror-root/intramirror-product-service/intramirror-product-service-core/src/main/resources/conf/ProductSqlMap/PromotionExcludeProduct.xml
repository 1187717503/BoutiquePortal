<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.intramirror.product.core.mapper.PromotionExcludeProductMapper">

    <resultMap id="BaseResultMap" type="com.intramirror.product.api.model.PromotionExcludeProduct">
        <id column="promotion_exclude_product_id" jdbcType="BIGINT" property="promotionExcludeProductId"/>
        <result column="promotion_id" jdbcType="BIGINT" property="promotionId"/>
        <result column="product_id" jdbcType="VARCHAR" property="productId"/>
    </resultMap>

    <sql id="Base_Column_List">
        promotion_exclude_product_id,promotion_id,product_id
    </sql>

    <select id="selectByParameter" parameterType="com.intramirror.product.api.model.PromotionExcludeProduct" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from `t_promotion_exclude_product` where 1=1

        <if test="promotionExcludeProductId != null">
            and `promotion_exclude_product_id`  =#{promotionExcludeProductId,jdbcType=BIGINT}
        </if>

        <if test="promotionId != null">
            and `promotion_id`  =#{promotionId,jdbcType=BIGINT}
        </if>

        <if test="productId != null">
            and `product_id`  =#{productId,jdbcType=BIGINT}
        </if>

    </select>

    <insert id="insertPromotionExcludeProduct" keyProperty="promotionExcludeProductId" parameterType="com.intramirror.product.api.model.PromotionExcludeProduct">
        insert into `t_promotion_exclude_product`(`promotion_id` ,`product_id` )values(#{promotionId,jdbcType=BIGINT},#{productId,jdbcType=BIGINT});
    </insert>

    <delete id="deletePromotionExcludeProduct" parameterType="java.lang.Long">
        delete from `t_promotion_exclude_product` where `promotion_exclude_product_id` = #{promotionExcludeProductId,jdbcType=BIGINT}
    </delete>

</mapper>